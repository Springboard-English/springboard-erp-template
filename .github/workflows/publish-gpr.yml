name: Publish Package

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://npm.pkg.github.com
          scope: "@Springboard-English"

      - name: Install dependencies
        run: npm ci

      # Before the build, so a broken pure function fails in seconds rather
      # than after vite, tsc and both dist checks have run.
      - name: Run tests
        run: npm test

      - name: Build package
        run: npm run build

      - name: Type check
        run: npx tsc

      - name: Publish to GitHub Packages
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
